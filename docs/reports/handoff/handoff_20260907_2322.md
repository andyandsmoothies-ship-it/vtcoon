# HANDOFF DOCUMENT — VTCoon Slice 01 -> Slice 02

**Thoi gian:** 2026-09-07T23:22:04+07:00
**Phien lam viec:** Session Slice 01 (hoan thanh)
**Phien tiep theo:** Thi cong Slice 02 — Bat Dong San Dat Nen & Thu Tien Thue Co Ban

---

## 1. Trang Thai Hien Tai

### Slice 00: DONE (truoc phien nay)
- board_config.ts, session_manager.ts, game_canvas.tsx (goc)
- 22/22 tests PASS

### Slice 01: DONE (phien nay)
- **Kiem toan:** [APPROVED] ca 2 cong Spec-Reviewer va Code-Reviewer
- **Bien ban:** docs/reports/audits/GAME-S01_acceptance_report.md
- **61/61 tests PASS | npx tsc --noEmit: 0 loi**

---

## 2. File Da Thi Cong (Slice 01)

| File | Hanh dong | LOC | Mo ta |
|:---|:---:|:---:|:---|
| src/domain/dice.ts | MOI | 34 | mulberry32 PRNG + rollDice 2D6 |
| src/domain/room.ts | MOI | 60 | TurnPhase, Player, Room, createRoom, checkPassedGo |
| src/domain/theme.ts | MOI | 16 | BOARD_SURFACE + PLAYER_TOKEN_PALETTE (design tokens) |
| src/server/room_manager.ts | MOI | 76 | RoomManager FSM: createRoom, joinRoom, startGame, handleRollDice |
| src/client/game_canvas.tsx | SUA | 56 | Them TokenMesh + prop players, dung design tokens |
| tests/domain/dice.test.ts | MOI | 57 | 7 tests: mulberry32 + rollDice |
| tests/domain/room.test.ts | MOI | 102 | 16 tests: createRoom, createPlayer, checkPassedGo |
| tests/server/room_manager.test.ts | MOI | 178 | 16 tests: FSM full loop TC-01.1..TC-01.5 |

**Tong LOC nguon Slice 01:** 242 / 400 ngan sach

---

## 3. Kien Truc Hien Hanh

### src/domain/dice.ts
- `mulberry32(seed: number): () => number` — Mulberry32 PRNG xac dinh
- `interface DiceResult { die1, die2, total }`
- `rollDice(rng): DiceResult` — goi rng() 2 lan, [1..6] moi vien

### src/domain/room.ts
- `enum TurnPhase { WaitingRoll, TurnEnd }` (Moving da xoa - YAGNI)
- `interface Player { id, position, balance }`
- `interface Room { roomCode, hostId, players, currentPlayerIndex, phase, started }`
- Constants: `BOARD_SIZE=40, GO_BONUS=2_000, INITIAL_BALANCE=15_000, ROOM_CODE_LENGTH=6`
- `generateRoomCode()`, `createPlayer(id)`, `createRoom(hostId)`, `checkPassedGo(old, new)`
- Logic: `checkPassedGo = newPos <= oldPos && oldPos !== newPos`

### src/domain/theme.ts
- `BOARD_SURFACE = '#2a2a3e'`
- `PLAYER_TOKEN_PALETTE` — 6 mau van hoa Viet Nam (Do Do, Xanh Da Troi, Xanh Luc Bao, Cam Nang, Tim Hoang Gia, Vang Anh Kim)

### src/server/room_manager.ts
- `interface RollResult { dice, player: {id, position, balance}, passedGo }`
- `class RoomManager(seed?: number)`:
  - `createRoom(hostId)` → luu vao Map<roomCode, Room>
  - `joinRoom(roomCode, playerId)` → tra undefined neu started hoac khong ton tai
  - `startGame(roomCode)` → can >= 2 nguoi, started=true
  - `handleRollDice(roomCode, playerId)` → guard: dung luot + WaitingRoll
  - `getRoom(roomCode)` → Room | undefined
- Xoay luot: `(currentPlayerIndex + 1) % players.length`
- GO bonus: `if (checkPassedGo(old, new)) balance += GO_BONUS`

### src/client/game_canvas.tsx
- `cellPosition(index)` — gia nguyen tu Slice 00
- `TokenMesh({ player, index })` — sphere tai cellPosition, dung PLAYER_TOKEN_PALETTE
- `GameCanvas({ players = [] })` — prop co default []

---

## 4. Slice 02 — Viec Can Lam Tiep Theo

**Tham khao:** docs/epics/gameplay/_epic_ledger.md (Slice 02: Pending)

**Muc tieu:** Bat Dong San Dat Nen & Thu Tien Thue Co Ban
- UC-GAME-020: Mua dat nen cap 0 (gia niem yet, tru quy)
- UC-GAME-027: Thu phi dung chan (chuyen tien sang chu)
- UC-GAME-028: Giao dich P2P song phuong (thue chuyen nhuong 5%)
- UC-GAME-029: Quan ly quyen so huu BDS

**Test Contracts can viet:**
- TC-02.1: Nguoi choi dung tai o dat trong -> mua voi gia niem yet
- TC-02.2: Doi thu dam vao o dat co chu -> tu dong thu phi
- TC-02.3: Thoa thuan mua ban P2P -> thue chuyen nhuong 5% vao Kho bac

**File can tao moi:**
- `src/domain/property.ts` — PropertyState, TitleDeed interface, gia tri 28 o dat
- `src/server/property_manager.ts` — buyProperty, collectRent, transferProperty
- `tests/domain/property.test.ts` + `tests/server/property_manager.test.ts`

**File can sua:**
- `src/server/room_manager.ts` — handleRollDice phai goi PropertyManager khi dung tai o dat
- `src/domain/room.ts` — co the them OwnedProperties map vao Room

---

## 5. Rang Buoc Ky Thuat Quan Trong

- **TurnPhase.Moving:** Da xoa, KHONG them lai khi chua co requirement cu the
- **checkPassedGo(old, new):** `newPos <= oldPos && oldPos !== newPos` (khong dung `<`)
- **GO thuế lũy tiến:** CHUA implement (chi co GO_BONUS=2000). Slice 02 co the them neu co BDS
- **Design tokens:** Moi mau sac moi PHAI them vao src/domain/theme.ts, KHONG hardcode hex
- **vitest.config.ts:** `globals: false` → PHAI import `{ describe, test, expect } from 'vitest'`
- **TypeScript:** `strict: true`, `noUncheckedIndexedAccess: true` → dung type-safe narrowing thay `!`
- **LOC:** max 400/file, max 30 dong/ham, CC <= 5

---

## 6. Ket Qua Kiem Toan (Da Xong)

- **Spec-Reviewer:** APPROVED — scope sach, 5/5 TCs, Failure Postconditions dung
- **Code-Reviewer:** APPROVED — sau khi:
  1. Tao theme.ts va xoa 7 hex hardcoded
  2. Xoa TurnPhase.Moving
  3. Thay `!` bang type-safe narrowing trong tests
  4. Them UC tags vao 2 describe blocks

**Bien ban chinh thuc:** docs/reports/audits/GAME-S01_acceptance_report.md

---

## 7. Ky Nang Nen Kich Hoat (Phien Tiep Theo)

Khi bat dau phien Slice 02, agent moi nen doc cac skill sau:
- `tdd` — Ping-Pong TDD: QA Tester viet test DO truoc, Implementer viet code XANH, Inversion Gate
- `use-case-slicing` — De tao ticket GAME-S02
- `writing-plans` — De lap ke hoach micro-tasks
- `vertical-slice-completeness` — Kiem tra khong bo sot layer khi them PropertyState
- `atdd-quality-gates` — Dam bao test infrastructure-clean

---

## 8. Lenh Huu ICH

```bash
# Chay toan bo test suite
npx vitest run

# Kiem tra TypeScript
npx tsc --noEmit

# Chay rieng test Slice 01
npx vitest run tests/domain/dice.test.ts tests/domain/room.test.ts tests/server/room_manager.test.ts
```

---

*Handoff duoc tao tu dong boi skill /handoff.*
